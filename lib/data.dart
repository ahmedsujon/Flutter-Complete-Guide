import 'package:flutter/material.dart';

class data extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text("Call Page", style: TextStyle(fontSize: 30, fontWeight: FontWeight.bold),),
        ),
    );
  }
}
