import 'package:flutter/material.dart';

class tabone extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text("Tab One Page"),
        ),
    );
  }
}
